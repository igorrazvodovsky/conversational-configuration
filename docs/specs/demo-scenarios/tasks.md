# Demo scenarios — tasks

Blocked until requirements.md is approved.

- [ ] Python mirror of the structured message grammar (choice/repair/adopt messages) for the harness
- [ ] Harness scaffolding: turn runner over graph.invoke, payload extraction, `scenario` pytest marker + skip wiring
- [ ] Scenario 1 — needs, not nomenclature (hospital): harness + assertions incl. choice provenance and one-turn override of an agent-picked value
- [ ] Scenario 2 — revision with repair (modernization → 3.0 m/s): harness + assertions incl. abandon path
- [ ] Scenario 3 — parallel candidates (practical vs premium): harness + assertions
- [ ] Scenario 4 — renewal as revision (state carry-over + what's-left + shaft-dimension entry): harness + assertions
- [ ] (blocked on environmental-footprint landing) footprint-delta-beside-price assertion in scenario 3
- [ ] `docs/demo-scenarios.md` presenter document, written against the running app
- [ ] Full manual pass of the presenter document in the browser
- [ ] Reconcile specs; update docs/specs/README.md roadmap status
