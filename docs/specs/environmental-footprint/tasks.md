# Environmental footprint — tasks

Requirements approved 2026-08-13; design drafted 2026-08-13; implemented and browser-passed 2026-08-13.

- [x] Model data: `co2` on every option of every variable (bottom-up values from [embodied-carbon.md](../../research/footprint/embodied-carbon.md); travel/stops/platform structural terms; platform calibrated last against the EPD anchor), `energy_package` + `energy_class` in the platform group, R35–R40, `footprint` block with assumptions and the class × usage × travel `annual_kwh` table derived from the ISO 25745-2 simplified formulas
- [x] Loader (`model.py`): `co2` + `footprint` block parsing; errors on missing `co2`, incomplete `annual_kwh` domain coverage
- [x] Footprint arithmetic: `ProductModel.footprint(assignment)` (embodied × multiplier, use-phase lookup, decarbonising bookend, integer kg); unit tests
- [x] Solver: `complete(objective="co2")` — embodied If-sum + 60-cell ternary use-phase term, lexicographic monthly tie-break, shorter-term tie; unit tests incl. brute-force cross-check
- [x] Validator: class determinacy, R35 conflict scenario, standby-inversion and conditional-regeneration shape checks, class monotonicity, embodied calibration vs the ~8.5 t anchor; passes with zero dead options
- [x] State/tools: `footprint` on candidate and frames ("—" fallback for pre-footprint threads), `propose_completion` objective parameter + other-objective teaser line, `frame_comparison` per-side footprint + `footprintDelta`, `describe_product` embodied deltas + assumptions section, `get_configuration` footprint line; unit tests updated
- [x] System prompt: claims discipline (modelled-not-certified, comparative/conditional phrasing, prohibited vocabulary, assumptions on request), cheapest/greenest pair flow via frames
- [x] Frontend: `Footprint` type + `formatCO2` + footprint block export in `configurator.ts`, canvas header lifetime figure + assumptions popover, footprint delta in `frame-comparison.tsx`
- [x] Browser pass (constitution #9): travel-band-vs-finish disagreement case, eco-package inversion (residential low vs office heavy), cheapest/greenest frame comparison, assumptions probe in chat, vocabulary spot-check — findings in [design.md](design.md) notes; one pre-existing off-spec issue (agent state bleeding into a new conversation) belongs to [nonlinear-interaction](../nonlinear-interaction/requirements.md), not here
- [x] Anchor: update this file, reconcile requirements/design with what was built, update the feature index in [specs/README](../README.md), reconcile the [product-model](../product-model/design.md), [solver-service](../solver-service/design.md), [agent-tools](../agent-tools/design.md) and [configuration-canvas](../configuration-canvas/design.md) designs, update the evidence line of [two objectives held as a pair](../../discovery/assertions/two-objectives-as-a-pair.md)
